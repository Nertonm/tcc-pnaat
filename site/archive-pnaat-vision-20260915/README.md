# Arquivo do runtime pnaat-vision

Preservado em 2026-09-15 antes da adoção do mock remoto em `site/`.

Fonte: `src-production/pnaat-vision/` do worktree local, capturada como 10 arquivos e listada em
`SHA256SUMS`. Esta cópia é histórica e não é servida, implantada, nem autoridade para promoção.
O runtime ativo continua fora deste diretório e exige seu próprio canário/rollback.

Motivo: o `site/` remoto é um draft estático; ele não substitui sem equivalência verificada a aplicação
de visão que existia localmente.
