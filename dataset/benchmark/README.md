# benchmark/: terceiro, para comparar metodo

`mvtec/` (MVTecAD, ~5,0 GB) e referencia publica: serve para responder "o pipeline esta bom?" sem
depender do nosso dado. So o subconjunto `bottle/` e usado.

Nao versionado. Registre a versao do benchmark quando reportar numero, senao a comparacao entre
rodadas nao vale nada.
